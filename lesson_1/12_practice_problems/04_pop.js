// what is the return of this statement? why?
console.log(['ant', 'bear', 'caterpillar'].pop().length);

// pop removes the last element and returns it. So pop should return the string caterpillar.
// Strings have a length property
// This should return the length of the last element

